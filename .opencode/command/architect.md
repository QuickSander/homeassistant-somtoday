---
description: Design the SomToday integration architecture and write docs/architecture.md
agent: architect
---

Design the architecture for the SomToday integration according to
`docs/architecture.md` and the architect-agent role in `AGENTS.md`.

Requirements and focus for this run:

$ARGUMENTS

If no specific requirements are given, use the default: a DataUpdateCoordinator
that polls every 15 minutes. Describe the config flow, coordinator, sensor
entities and error handling, and write the result to `docs/architecture.md`.
